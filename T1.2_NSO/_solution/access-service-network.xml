<config-template xmlns="http://tail-f.com/ns/config/1.0">
  <devices xmlns="http://tail-f.com/ns/ncs">
    <device>
      <!--
          Select the devices from some data structure in the service
          model. In this skeleton the devices are specified in a leaf-list.
          Select all devices in that leaf-list:
      -->
      <name>{/device}</name>
      <!--
        TODO: Modify this sample device configuration to create the configuration
              rendered based on the access-service definition.
      -->
      <config>
        <vlan xmlns="urn:ios">
          <vlan-list>
            <id>{$VLAN_ID}</id>
            <name>{$VLAN_DESCRIPTION}</name>
          </vlan-list>
        </vlan>
        <interface xmlns="urn:ios">
          <Vlan>
          <name>{$VLAN_ID}</name>
            <ip>
              <address>
                <primary>
                  <address>{$VLAN_NETWORK_ADDRESS}</address>
                  <mask>{$VLAN_NETWORK_MASK}</mask>
                </primary>
              </address>
            </ip>
            <shutdown tags="delete" />
          </Vlan>
        </interface>
      </config>
    </device>
  </devices>
</config-template>
